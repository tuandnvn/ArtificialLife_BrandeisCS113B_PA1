Run the test.py file:

python test.py
Usage: test.py [OPTIONS]

Options:
  -h, --help            show this help message and exit

  -n SIZE, --size=SIZE  Number of tests

  -c CROSSOVER, --crossover=CROSSOVER
                        Crossover rate

  -m MUTATION, --mutation=MUTATION
                        Mutation rate

  -s SUITMUTATION, --suitmutation=SUITMUTATION
                        Suit mutation rate

  -e ELITISM, --elitism=ELITISM
                        Elitism rate

